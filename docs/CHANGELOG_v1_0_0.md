# SARAKURA v1.0.0

- Added `schema export` and v1.0 schema bundle generation.
- Added `validate --strict` with cross-field validation.
- Added optional-metadata `emitter-check` plus strict compatibility warnings.
- Added unknown event suggestions to catalog coverage.
- Added `inspect-repro` for repro bundle manifests and entries.
- Added tool capability filtering for `automation-plan`.
- Added E2E example fixture layout under `examples/gb/e2e` and `examples/fc/e2e`.
- Updated repro bundles to include `diagnostic_summary.json` and a richer manifest.

